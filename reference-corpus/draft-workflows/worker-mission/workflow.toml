# Draft workflow — N1 reference corpus (docs/gauntlet/contracts/N1.md).
# Not admitted: lives under .sergeant/drafts/workflows/ per
# docs/icm/convention.md §2. Promotion to .sergeant/workflows/ is a
# distinct, human-reviewed act.

[workflow]
name = "worker-mission"
version = "2"
stages = ["10-triage-and-route", "20-implement", "30-independent-review", "40-escalate-or-continue"]

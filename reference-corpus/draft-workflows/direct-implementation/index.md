---
kind: workflow
name: direct-implementation
status: draft
version: 1
description: >-
  Implement in the current session, against one owning repository, under the same delivery contract as a dispatched worker.
tags:
  - implementation
  - single-repository
---

# Direct Implementation

Draft workflow candidate (N1 reference corpus, not admitted procedure —
see `docs/icm/convention.md` §2). Use when: The user explicitly asks to work in this session, and one repository owns the complete outcome.

See `CONTEXT.md` for workflow orientation, `workflow.toml` for the pinned
stage order, and `provenance.md` for the full behavior-unit citations.

# Draft workflow — N1 reference corpus (docs/gauntlet/contracts/N1.md).
# Not admitted: lives under .sergeant/drafts/workflows/ per
# docs/icm/convention.md §2. Promotion to .sergeant/workflows/ is a
# distinct, human-reviewed act.

[workflow]
name = "respond-to-worker"
version = "1"
stages = ["00-precondition-check", "10-validate-target", "20-publish-response", "30-deliver-and-accept", "40-apply-and-acknowledge", "50-archive-evidence", "60-notify-coordinator", "70-relaunch-if-needed"]

---
kind: workflow
name: deepen-module
status: draft
version: 1
description: >-
  Turn a shallow module into a deep one at a deliberately chosen seam.
tags:
  - module-design
  - architecture
  - seams
---

# Deepen Module

Draft workflow candidate (N1 reference corpus, not admitted procedure —
see `docs/icm/convention.md` §2). Use when: A module's interface needs redesign, or a port/adapter decision needs to be made deliberately rather than by default.

See `CONTEXT.md` for workflow orientation, `workflow.toml` for the pinned
stage order, and `provenance.md` for the full behavior-unit citations.

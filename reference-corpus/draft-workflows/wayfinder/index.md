---
kind: workflow
name: wayfinder
status: draft
version: 2
description: >-
  Map an unfamiliar frontier of a codebase or problem space, ticket-izing decisions and resolving them one at a time.
tags:
  - exploration
  - mapping
  - tickets
---

# Wayfinder

Draft workflow candidate (N1 reference corpus, not admitted procedure —
see `docs/icm/convention.md` §2). Use when: A destination is named that requires mapping fog before it can be reached.

See `CONTEXT.md` for workflow orientation, `workflow.toml` for the pinned
stage order, and `provenance.md` for the full behavior-unit citations.

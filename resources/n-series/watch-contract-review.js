export const meta = {
  name: 'watch-contract-review',
  description: 'L19 fresh-eyes review of docs/gauntlet/contracts/WATCH.md before it governs the build',
  phases: [
    { title: 'Panel', detail: '2 Sonnet critics: fidelity-to-record, buildability' },
  ],
}
const REPO = '/home/miztertea/sergeant-rs'
const COMMON = `Blind critic reviewing a GOVERNING CONTRACT before it governs a build (L19). Artifact: ${REPO}/docs/gauntlet/contracts/WATCH.md (on branch cerberus/watch). It derives from ${REPO}/reference/proposal-sgt-watch-v1.md as amended by an already-run proposal-review gauntlet and owner rulings. Read-only in ${REPO} (cargo test / sgt --help allowed with PATH="$HOME/.cargo/bin:$PATH"; NEVER edit; never leave daemons running). Read FIRST: the contract, the proposal, GAUNTLET.md's deviation register (L3: don't re-litigate settled rulings — the six-state set, the fingerprint identity, no-auto-spawn, attach-then-reconcile, and the pinned-test revision are ADJUDICATED; a finding against one must argue the ruling wrong with new evidence, not that it differs from the proposal), LESSONS.md, NORTH-STAR.md. Findings carry file:line / section / ruling-id evidence. Severity: error (a builder following this contract ships a defect, a false claim, or an untestable pin), warning (ambiguity needing one more sentence or a wrong citation), info.`
const FINDINGS = { type: 'object', required: ['findings', 'summary'], properties: { summary: { type: 'string' }, findings: { type: 'array', items: { type: 'object', required: ['id', 'severity', 'claim', 'evidence'], properties: { id: { type: 'string' }, severity: { type: 'string', enum: ['error', 'warning', 'info'] }, claim: { type: 'string' }, evidence: { type: 'string' } } } } } }
phase('Panel')
const axes = [
  { key: 'fidelity', brief: `Axis FIDELITY-TO-RECORD: (1) verify every source citation in the contract is real and says what the contract claims (engine.rs retry gate lines, work.rs event-kind lines, api.rs work_view keys and decode_frame test names/lines, cli.rs exemption comments, fake.rs SGT_FAKE_SCRIPT) — a governing doc with a wrong line number sends a builder to the wrong code; (2) check the contract against the review docket's confirmed findings: does every confirmed error/warning from the proposal review land in a ruling or get an explicit disposition, or did any confirmed finding silently vanish (the docket's finding ids: V1,V2,V3,F1..F7,orch F1/F2/F5 — the docket summary lives in this contract's own preamble; the vendored proposal + contract must jointly cover them); (3) do any two rulings contradict each other or the proposal text they leave standing (e.g. R-WATCH-4's attach-then-reconcile vs the proposal's §6.6 text that stays; R-WATCH-3 vs §11.1's retained half; Acceptance section's §17 substitutions — is any OTHER §17 criterion now false under the rulings but not listed as substituted)?` },
  { key: 'buildability', brief: `Axis BUILDABILITY: could a Sonnet builder implement from this contract + proposal alone without a judgment call the contract should have made? Probe hardest: (1) R-WATCH-2's detail_identity — is 'hash of the snapshot's current question/blocking-detail text' implementable as specified: where in the GET /v1/work/{id} body does that text actually live for needs_input vs blocked vs waiting vs failed (read work_view in src/api.rs and the domain types), and is it well-defined for ALL six states (what does a completed snapshot hash)? (2) R-WATCH-6's SGT_WATCH_TEST_HOLD protocol — is the touch-.ready-then-block-until-path-exists handshake unambiguous (who creates the parent dir, what polling interval, does it dead-man out or block forever, what happens in --follow mode on later iterations)? (3) R-WATCH-3's remedy text — does 'start one with any dispatching verb or sgt daemon' match real verbs, and how does watch DETECT no-daemon-running without ensure_daemon (what does the existing code offer: runtime.json probe? connect-refused? read what doctor and daemon-stop do)? (4) R-WATCH-9's deep-equals test — implementable given the notice embeds the body at emission time while the Work may have changed by assertion time? (5) R-WATCH-8 + R-WATCH-1 combined: waiting/blocked/failed all emit-and-continue in scoped --follow — is the termination story coherent and testable? (6) Is anything the contract calls a Pin unfalsifiable as written (L7)?` },
]
const crits = await parallel(axes.map(a => () => agent(`${COMMON}\n${a.brief}`, { label: `critic:${a.key}`, phase: 'Panel', model: 'sonnet', effort: 'high', schema: FINDINGS })))
const all = crits.flatMap((c, i) => (c ? c.findings.map(f => ({ ...f, id: `${axes[i].key}:${f.id}` })) : []))
log(`panel: ${all.length} findings (${all.filter(f => f.severity === 'error').length} error / ${all.filter(f => f.severity === 'warning').length} warning)`)
return { findings: all, summaries: crits.filter(Boolean).map(c => c.summary) }
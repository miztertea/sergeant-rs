# deepen-module — admitted workflow (N1, sergeant-rs-workspace/knowledge/evidence/gauntlet/contracts/N1.md,
# candidate W25). Turns a shallow module into a deep one at a deliberately
# chosen seam: classifies the dependency cluster, generates and compares
# independently designed interfaces, and replaces shallow-module tests with
# tests at the new interface.
#
# Three ordinary actor stages (ladder §6.4, judgment required at each).
# Stage order here is authoritative; the directory listing under this
# workflow's own tree MUST agree with it lexically (docs/icm/convention.md
# §1 rule 4).
#
# Promoted per docs/icm/promotion-spec-2026-08-11.md; the full
# behavior-unit citation trail is archived at
# sergeant-rs-workspace/knowledge/evidence/gauntlet/promoted-provenance/deepen-module.md.

[workflow]
name = "deepen-module"
version = "1"
stages = ["00-classify-dependencies", "10-design-it-twice", "20-test-at-new-interface"]

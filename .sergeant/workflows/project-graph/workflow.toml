# project-graph — admitted workflow (N1, docs/gauntlet/contracts/N1.md,
# candidate W2). Produces exactly one merged, published graph per project,
# outside every source repository, usable for architecture questions.
#
# Two ordinary actor stages (10-extract-per-repo and 30-publish-atomically
# fold into 20-merge-or-fail per N1 adjudication A4 — see the archived
# provenance). Stage order here is authoritative; the directory listing
# under this workflow's own tree MUST agree with it lexically
# (docs/icm/convention.md §1 rule 4).
#
# Curated for promotion per docs/icm/promotion-spec-2026-08-11.md; the full
# stage-to-behavior-unit citation trail is archived at
# docs/gauntlet/promoted-provenance/project-graph.md.

[workflow]
name = "project-graph"
version = "2"
stages = ["00-resolve-output-path", "20-merge-or-fail"]

# tdd — admitted workflow (N1, docs/gauntlet/contracts/N1.md, candidate
# W22). Test-driven development for one confirmed seam at a time: red,
# green, one minimal implementation. Promoted per
# docs/icm/promotion-spec-2026-08-11.md; full behavior-unit citation trail
# archived at docs/gauntlet/promoted-provenance/tdd.md.
#
# Two ordinary actor stages. Stage order here is authoritative; the
# directory listing under this workflow's own tree must agree with it.

[workflow]
name = "tdd"
version = "1"
stages = ["00-agree-seams", "10-red-green-cycle"]

# vet-external-skill — admitted workflow (N1, docs/gauntlet/contracts/N1.md,
# candidate W34). Vets an external skill through a fixed sequence before
# adopting it, and keeps already-adopted skills updated through the same
# discipline. Promoted per docs/icm/promotion-spec-2026-08-11.md; full
# behavior-unit citation trail archived at
# docs/gauntlet/promoted-provenance/vet-external-skill.md.
#
# Seven ordinary actor stages. Stage order here is authoritative; the
# directory listing under this workflow's own tree must agree with it.

[workflow]
name = "vet-external-skill"
version = "2"
stages = ["00-read-source", "10-confirm-provenance", "20-check-actions", "30-verify-no-conflict", "50-test-in-disposable-copy", "60-update-managed", "60-update-owned"]

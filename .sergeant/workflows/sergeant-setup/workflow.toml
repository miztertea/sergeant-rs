# sergeant-setup — admitted workflow (N1 candidate W3,
# docs/gauntlet/contracts/N1.md). Captures a complete project definition
# through interview and tracks capability gaps discovered along the way.
#
# Two ordinary actor stages, down from the original eight per the MVP-5 F2
# execution-surface re-triage SPLIT verdict (docs/icm/retriage-2026-08-11.md):
# 00-detect-prerequisites/10-install-commands/20-global-config/
# 40-repair-existing/60-task-tracking-init/70-optional-capabilities retired
# as CLI-SURFACE, absorbed by shipped `sgt init`/`sgt doctor` — see
# docs/icm/re-homing-record-2026-08-12.md. Stage order here is authoritative;
# the directory listing under this workflow's own tree MUST agree with it
# lexically (docs/icm/convention.md §1 rule 4).
#
# Originally promoted per docs/icm/promotion-spec-2026-08-11.md. The full
# behavior-unit citation trail for all eight original stages (and the N1
# Adjudication A4 reasoning) is archived at
# docs/gauntlet/promoted-provenance/sergeant-setup.md.

[workflow]
name = "sergeant-setup"
version = "3"
stages = ["05-file-capability-gaps", "30-project-interview"]

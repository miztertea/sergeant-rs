# reconcile-and-cleanup-fleet — admitted workflow (N1,
# docs/gauntlet/contracts/N1.md, candidate W15). Requires every targeted
# repo safely terminal and its task verifiably closed, then reconciles
# fleet status, re-verifies ownership, confirms and seals handshake
# acknowledgement, removes each repo's surface, and retires whole-task
# state once every repo is done.
#
# One actor stage (N1 adjudication A4 folded five deterministic-machinery
# stages into it as helper invocations, including two mutating stages
# moved in from monitor-fleet under A7 — see the archived provenance).
# Stage order here is authoritative; the directory listing under this
# workflow's own tree MUST agree with it lexically (docs/icm/convention.md
# §1 rule 4).
#
# Curated for promotion per docs/icm/promotion-spec-2026-08-11.md; the full
# stage-to-behavior-unit citation trail is archived at
# docs/gauntlet/promoted-provenance/reconcile-and-cleanup-fleet.md.

[workflow]
name = "reconcile-and-cleanup-fleet"
version = "2"
stages = ["00-require-terminal"]

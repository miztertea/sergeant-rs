# load-project — admitted workflow (N1, docs/gauntlet/contracts/N1.md,
# candidate W1). Establishes, before any mutation, which repositories own
# the requested outcome, where they are, what instructions govern them, and
# what state they are in.
#
# Three ordinary actor stages. Stage order here is authoritative; the
# directory listing under this workflow's own tree MUST agree with it
# lexically (docs/icm/convention.md §1 rule 4).
#
# Curated for promotion per docs/icm/promotion-spec-2026-08-11.md; the full
# stage-to-behavior-unit citation trail is archived at
# docs/gauntlet/promoted-provenance/load-project.md.

[workflow]
name = "load-project"
version = "2"
stages = ["00-resolve-project-name", "10-resolve-context", "20-register-or-edit"]

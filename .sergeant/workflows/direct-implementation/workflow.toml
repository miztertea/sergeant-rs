# direct-implementation — admitted workflow (N1 reference corpus,
# docs/gauntlet/contracts/N1.md; candidate W6,
# reference-corpus/synthesis.md §1). Implements in the current session,
# against one owning repository, under the same delivery contract as a
# dispatched worker. Promoted per docs/icm/promotion-spec-2026-08-11.md;
# full behavior-unit citation trail archived at
# docs/gauntlet/promoted-provenance/direct-implementation.md.
#
# Five ordinary actor stages (N1 adjudication A4 folded two demoted
# checkpoints — 02-reconcile-existing-state into 03-claim-and-implement,
# 07-record-outcomes into 06-pr-and-merge — see the archived provenance
# for the full fold record). `05-shipping-gate` delegates to the promoted
# `validate-and-ship` workflow. Stage order here is authoritative; the
# directory listing under this workflow's own tree MUST agree with it
# lexically (docs/icm/convention.md §1 rule 4).

[workflow]
name = "direct-implementation"
version = "2"
stages = ["01-load-task-context", "03-claim-and-implement", "04-validate", "05-shipping-gate", "06-pr-and-merge"]

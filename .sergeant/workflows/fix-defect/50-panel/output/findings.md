# Panel findings — #234 dirty-patch trailing-newline corruption

## Panel disclosure

Four axis seats — `spec-fidelity`, `invariants`, `simplicity`,
`test-honesty` — were spawned as sub-agents in a single message, in
parallel, each given a self-contained brief (issue #234's report and
follow-up evidence, the confirmed root cause from `30-instrument`, and
the fix diff from `40-fix-with-regression-test`, commit `56927a14`) and a
400-word cap. All four completed; no axis is missing or degraded.

Per `@@panel`/`@@fan-out-evidence`: these are four isolated briefs read by
one stage execution, not four independent reviews. Isolation here is
context isolation only — each seat saw only its own brief and nothing of
the other three seats' reasoning — and it is weaker than the isolation a
separate Work would give: all four share this stage's single execution,
journal entry, usage window, and failure mode. Every finding below enters
at `status: raised`. This stage does not confirm or refute; confirmation
is `55-refute`'s act alone.

## Typed finding set

| id | axis | claim | evidence | severity | status | refutation |
|---|---|---|---|---|---|---|
| F-SF-01 | spec-fidelity | Core ask (trailing-newline byte-exactness for `capture_dirty_patch`) is met precisely: `git_bytes` returns `output.stdout` unmodified and `capture_dirty_patch` writes it via `write_atomic(&path, &diff)` with no String round-trip. | `git.rs` new `git_bytes` fn (no `.trim()`, no UTF-8 conversion) and `surface.rs` diff replacing `git(...).ok()?.as_bytes()` with `git_bytes(...).ok()?` fed straight to `write_atomic`. | low | raised | |
| F-SF-02 | spec-fidelity | Regression coverage matches the issue's own explicit acceptance ask almost verbatim: both `git apply --check` and `git apply --stat` are run, and the unit test's dirtied file deliberately ends its diff content in `]`, mirroring the follow-up comment's "ends with punctuation or a closing bracket" ask. | `retain_dirty_writes_a_patch_git_apply_accepts` in `surface.rs`; `walk_for_marker` addition in `tests/m7_docker_executor.rs`. | low | raised | |
| F-SF-03 | spec-fidelity | H2 (non-UTF-8 lossy re-encoding) is fixed as a byproduct of the same `git_bytes` change and is not unasked-for scope — it was confirmed as an independent root cause during the hypothesis/instrument stages feeding this fix, and eliminating the String/UTF-8 round-trip entirely is the only way to make the write byte-exact. | Fix commit message: "no trim, no UTF-8 round-trip"; doc comment on `git_bytes` cites both defects. | low | raised | |
| F-SF-04 | spec-fidelity | No regression test actually exercises H2 (a diff containing non-UTF-8/binary bytes). The only test content is plain ASCII text; the claim that the fix also resolves the confirmed lossy-re-encoding defect is asserted in the commit message and doc comment but never verified by an assertion that a non-UTF-8 byte survives capture unchanged. | `retain_dirty_writes_a_patch_git_apply_accepts` only writes ASCII (`"const V: [u8; 2] = [0, 1]"`, `"fn wanted() {}\n"`); no fixture contains a raw non-UTF-8 byte. | medium | raised | |
| F-SF-05 | spec-fidelity | The `git status --porcelain` call site at `surface.rs:1980` is explicitly carved out as "separate-issue candidate" rather than filed as a linked follow-up issue, despite sharing the same root-defect class this fix just diagnosed and generalized in `git_bytes`'s doc comment. | Commit message: "Out of scope... the adjacent git() misuse for `git status --porcelain` at surface.rs:1980" — no issue number or tracker reference given. | low | raised | |
| F-IV-01 | invariants | `git_bytes`'s error path is byte-identical in shape/semantics to `git()`'s — both return `GitError::Failed { stderr: ...lossy...trim... }` on non-zero exit, and `capture_dirty_patch`'s `.ok()?` swallows both identically. No caller-visible contract changed on the failure path. | src/runtime/git.rs:175-192 vs 116-133; src/runtime/surface.rs:1677 (`git_bytes(...).ok()?`) | low | raised | |
| F-IV-02 | invariants | `PatchInfo.bytes` is not purely cosmetic: `reap_binding`'s `RetainedDirty { patch: Some(info) }` arm reports `ReapOutcome::Reaped { bytes: info.bytes }` directly from the stored value, so the free-space-accounting number this fix changes for non-UTF-8 diffs (H2 case) now differs from what pre-fix code would have reported for the same repository state — a real, if correctness-improving, behavior change with no test asserting the accounting delta itself. | src/runtime/surface.rs:1486 (`ReapOutcome::Reaped { bytes: info.bytes }`), 1690-1693 (`bytes: diff.len() as u64`) | medium | raised | |
| F-IV-03 | invariants | `PatchInfo`'s serialized JSON schema is unchanged, so persisted-record deserialization compatibility holds — but any `.dirty.patch` file and its accompanying `PatchInfo.bytes` written by pre-fix code remain corrupt/mis-sized on disk after upgrade; nothing in this change detects or migrates already-retained state. | src/runtime/surface.rs:422-431 (`#[derive(..., Serialize, Deserialize)] pub struct PatchInfo`) | low | raised | |
| F-IV-04 | invariants | `write_atomic(&path, &diff)` receiving `&Vec<u8>` for a `&[u8]` parameter is safe (auto-deref coercion), and the write-tmp/fsync/rename/fsync-dir recipe is untouched, so atomicity/durability of the patch file write is preserved identically for the new byte-vector path. | src/runtime/fsutil.rs:105-137; src/runtime/surface.rs:1689 | low | raised | |
| F-IV-05 | invariants | `git_bytes` is not exercised by the injectable-`SGT_GIT_BIN` test process, even though it shares the same `command()` construction — a minor coverage gap rather than a proven defect, since the override mechanism is structurally identical to `git()`/`git_verbatim()`. | tests/c11_injectable_git.rs (no `git_bytes` reference); src/runtime/git.rs:175-191 (`command(dir, args)` shared helper) | low | raised | |
| F-SI-01 | simplicity | `git_bytes` duplicates the entire spawn/status-check/`GitError::Spawn`/`GitError::Failed` block already present verbatim in `git()` and `git_verbatim()`, rather than factoring a shared internal helper that all three thin wrappers call. | src/runtime/git.rs: `git()`, `git_verbatim()`, `git_bytes()` are now three copies of the same ~10-line block, differing only in the final conversion line. | medium | raised | |
| F-SI-02 | simplicity | The fix could have been made smaller by giving `git()`/`git_verbatim` a raw-bytes exit point reused by a one-line `git_bytes`, rather than adding a fully independent public function with its own doc comment and error handling. | Diff adds a whole new ~28-line public function when the delta versus `git_verbatim` is exactly "don't call `.to_string()`, return `output.stdout` instead". | low | raised | |
| F-SI-03 | simplicity | The new unit test does far more than needed to catch this specific regression — independent temp dir, clone, checkout, `git apply --check` + `apply` + byte-for-byte content comparison of two files — when the regression is fully caught by asserting the last byte is `b'\n'` plus a single `git apply --check` in a lightweight scratch dir. | `retain_dirty_writes_a_patch_git_apply_accepts` in `src/runtime/surface.rs` (~90 new lines). | low | raised | |
| F-SI-04 | simplicity | The doc comment on `git_bytes` prescribes global caller behavior ("never `git` or `git_verbatim`... whenever...") for what is, per this diff, a single call site (`capture_dirty_patch`), more ceremony than the change's actual footprint justifies. | src/runtime/git.rs doc comment on `git_bytes`; only `surface.rs::capture_dirty_patch` was migrated in this commit. | low | raised | |
| F-TH-01 | test-honesty | Neither regression test exercises root cause H2 (lossy UTF-8 re-encoding corrupting non-UTF-8 diff bytes), despite it being cited as a second confirmed root cause of #234. | Test #1 fixture content is pure ASCII; test #2's markers are also ASCII text. `capture_dirty_patch` uses `git diff --cached --binary`, implying binary/non-UTF-8 content is an expected input class, but no fixture in either test contains non-UTF-8 bytes. | medium | raised | |
| F-TH-02 | test-honesty | Test #2's new `git apply --stat`/`--check` assertions are not guaranteed to execute — `walk_for_marker` can return `true` via a pre-existing plain-`validated.txt` branch without ever reaching the patch-checking branch, so the test can pass green without the new code path running. | The function has two independent match arms that each set `found`: a plain-file arm (untouched by the fix) and a `.patch`-content arm (where the new `git apply` checks live); no assertion pins that the patch arm specifically fired. | medium | raised | |
| F-TH-03 | test-honesty | The "watched failing first" claim (pre-fix capture rejected by `git apply --check`) is self-reported by the fix author and not independently verifiable from what this stage was given — no captured pre-fix output, diff, or stage log is attached. | Status note is explicitly flagged as self-reported/not independently verified; no artifact (failing CI run, saved stderr, revert-and-rerun log) accompanies it. | low | raised | |
| F-TH-04 | test-honesty | The full-suite `cargo test` "10 pre-existing environmental failures, none touching git.rs/surface.rs" claim is asserted, not demonstrated to this panel — no test names or log excerpt were included in the brief, and the causal attribution to ambient `SGT_*` env vars is unverified from what was provided. | Same status note, same self-reported caveat. | low | raised | |
| F-TH-05 | test-honesty | Test #1's assertion that reading the original worktree file returns `Err` is padding unrelated to proving the fix — it verifies teardown reclaimed the directory, not anything about patch correctness or the newline/UTF-8 bug. | The assertion is explicitly commented "sanity: the worktree must actually be gone by now," disconnected from the patch-validity assertions around it. | low | raised | |

19 findings (5 spec-fidelity, 5 invariants, 4 simplicity, 5 test-honesty),
all `status: raised`. Severity split: 5 medium (F-SF-04, F-IV-02, F-SI-01,
F-TH-01, F-TH-02), 14 low, 0 high.

## Seat reports, verbatim

### Seat: spec-fidelity

> ## Findings
>
> | id | axis | claim | evidence | severity | status | refutation |
> |---|---|---|---|---|---|---|
> | F-SF-01 | spec-fidelity | Core ask (trailing-newline byte-exactness for `capture_dirty_patch`) is met precisely: `git_bytes` returns `output.stdout` unmodified and `capture_dirty_patch` writes it via `write_atomic(&path, &diff)` with no String round-trip. | `git.rs` new `git_bytes` fn (no `.trim()`, no UTF-8 conversion) and `surface.rs` diff replacing `git(...).ok()?.as_bytes()` with `git_bytes(...).ok()?` fed straight to `write_atomic`. | low | raised | |
> | F-SF-02 | spec-fidelity | Regression coverage matches the issue's own explicit acceptance ask almost verbatim: both `git apply --check` and `git apply --stat` are run (unit test uses `--check` against an independent checkout; e2e `walk_for_marker` runs both `--stat` and `--check`), and the unit test's dirtied file deliberately ends its diff content in `]`, mirroring the follow-up comment's "ends with punctuation or a closing bracket" ask. | `retain_dirty_writes_a_patch_git_apply_accepts` in `surface.rs`; `walk_for_marker` addition in `tests/m7_docker_executor.rs`. | low | raised | |
> | F-SF-03 | spec-fidelity | H2 (non-UTF-8 lossy re-encoding) is fixed as a byproduct of the same `git_bytes` change and is not unasked-for scope — it was confirmed as an independent root cause during the hypothesis stage feeding this fix, and eliminating the String/UTF-8 round-trip entirely is the only way to make the write byte-exact, so it is inseparable from the newline fix rather than gold-plating. | Fix commit message: "no trim, no UTF-8 round-trip"; doc comment on `git_bytes` explicitly cites both defects. | low | raised | |
> | F-SF-04 | spec-fidelity | No regression test actually exercises H2 (a diff containing non-UTF-8/binary bytes, e.g. from `git diff --binary` on a binary file). The only test content is plain ASCII text ending in `]`; the claim that the fix also resolves the confirmed lossy-re-encoding defect is asserted in the commit message and doc comment but never verified by an assertion that a non-UTF-8 byte survives capture unchanged. | `retain_dirty_writes_a_patch_git_apply_accepts` only writes ASCII (`"const V: [u8; 2] = [0, 1]"`, `"fn wanted() {}\n"`); no test constructs a tracked/untracked file containing a raw non-UTF-8 byte (e.g. `0xFF`) and checks it round-trips byte-identical. | medium | raised | |
> | F-SF-05 | spec-fidelity | The `git status --porcelain` call site at `surface.rs:1980` is explicitly carved out as "separate-issue candidate" rather than filed as a linked follow-up issue. It shares the exact same root-defect class (using `git()`'s trim/lossy path where output bytes are structurally significant) that this fix just diagnosed and named as a pattern (`git_bytes`'s doc comment generalizes the rule), so leaving it as an unlinked prose note risks the same defect class recurring without traceable acceptance criteria. | Commit message: "Out of scope... the adjacent git() misuse for `git status --porcelain` at surface.rs:1980, where leading-column whitespace is significant" — no issue number or tracker reference given. | low | raised | |

### Seat: invariants

> ## Findings
>
> | id | axis | claim | evidence | severity | status | refutation |
> |---|---|---|---|---|---|---|
> | F-IV-01 | invariants | `git_bytes`'s error path is byte-identical in shape/semantics to `git()`'s — both return `GitError::Failed { stderr: String::from_utf8_lossy(&output.stderr).trim().to_string(), .. }` on non-zero exit, and `capture_dirty_patch`'s `.ok()?` swallows both identically. No caller-visible contract changed on the failure path. | src/runtime/git.rs:175-192 vs 116-133; src/runtime/surface.rs:1677 (`git_bytes(...).ok()?`) | low | raised | |
> | F-IV-02 | invariants | `PatchInfo.bytes` is not purely cosmetic: `reap_binding`'s `RetainedDirty { patch: Some(info) }` arm reports `ReapOutcome::Reaped { bytes: info.bytes }` directly from the stored value (not a fresh `directory_size`/`fs::metadata` read), so the free-space-accounting number this fix changes for non-UTF-8 diffs (H2 case) now differs from what pre-fix code would have reported for the same repository state — a real, if correctness-improving, behavior change in reported reaped bytes with no test asserting the accounting delta itself. | src/runtime/surface.rs:1486 (`ReapOutcome::Reaped { bytes: info.bytes }`), 1690-1693 (`bytes: diff.len() as u64`) | medium | raised | |
> | F-IV-03 | invariants | `PatchInfo` is `Serialize + Deserialize` (JSON schema unchanged: `{path, bytes: u64}`), so on-disk deserialization compatibility for persisted teardown/reap evidence holds — but any `.dirty.patch` file and its accompanying `PatchInfo.bytes` written by pre-fix code remain corrupt/mis-sized on disk after upgrade; nothing in this change detects or migrates already-retained state, so old and new records are silently indistinguishable to a reader. | src/runtime/surface.rs:422-431 (`#[derive(..., Serialize, Deserialize)] pub struct PatchInfo`) | low | raised | |
> | F-IV-04 | invariants | `write_atomic(&path, &diff)` receiving `&Vec<u8>` for a `&[u8]` parameter is safe (auto-deref coercion), and the underlying write-tmp/fsync/rename/fsync-dir recipe is untouched, so atomicity/durability of the patch file write is preserved identically for the new byte-vector path. | src/runtime/fsutil.rs:105-137; src/runtime/surface.rs:1689 | low | raised | |
> | F-IV-05 | invariants | `git_bytes` is not exercised by the injectable-`SGT_GIT_BIN` test process (`tests/c11_injectable_git.rs` only documents/tests `git`/other entry points), even though it shares the same `command()` construction — a minor coverage gap rather than a proven defect, since the override mechanism is structurally identical. | tests/c11_injectable_git.rs (no `git_bytes` reference); src/runtime/git.rs:175-191 (`command(dir, args)` shared helper) | low | raised | |

### Seat: simplicity

> Confirmed: `git()`, `git_verbatim()`, and the new `git_bytes()` are three near-identical ~10-line blocks (spawn, status check, error construction) that differ only in the final `Ok(...)` conversion. This is pre-existing duplication that the fix triples down on rather than factoring out.
>
> | id | axis | claim | evidence | severity | status | refutation |
> |---|---|---|---|---|---|---|
> | F-SI-01 | simplicity | `git_bytes` duplicates the entire spawn/status-check/`GitError::Spawn`/`GitError::Failed` block already present verbatim in `git()` and `git_verbatim()`, rather than factoring a shared `fn run(dir, args) -> Result<Output, GitError>` (or similar) that all three thin wrappers call | `src/runtime/git.rs`: `git()`, `git_verbatim()`, and `git_bytes()` are now three copies of the same ~10-line command/spawn/error block, differing only in the final line (`String::from_utf8_lossy(...).trim()` vs `.trim_end_matches('\n')` vs raw `output.stdout`) | medium | raised | |
> | F-SI-02 | simplicity | The fix could have been made smaller by giving `git()` (or a new small internal helper) a raw-bytes exit point that `git_verbatim` and a one-line `git_bytes` both reuse, rather than adding a fully independent public function with its own doc comment and error handling | Diff adds a whole new ~28-line public function to `src/runtime/git.rs` when the delta versus `git_verbatim` is exactly "don't call `String::from_utf8_lossy(...).to_string()`, return `output.stdout` instead" | low | raised | |
> | F-SI-03 | simplicity | The new unit test (`retain_dirty_writes_a_patch_git_apply_accepts`, ~90 lines) does far more than needed to catch this specific regression — it spins up an independent temp dir, clones the source repo, checks out a base SHA, and does a full `git apply --check` + `git apply` + byte-for-byte content comparison of two files, when the regression (trailing byte dropped) is fully caught by asserting `patch_bytes.last() == Some(b'\n')` plus a single `git apply --check` in a lightweight scratch dir | Test body in `src/runtime/surface.rs` (diff shows ~90 new lines) performs clone + checkout + apply --check + apply + two separate content assertions, substantially more machinery than the byte-dropped-newline bug requires to reproduce | low | raised | |
> | F-SI-04 | simplicity | The doc comment on `git_bytes` prescribes global caller behavior ("never `git` or `git_verbatim`... whenever...") for what is, per this diff, a single call site (`capture_dirty_patch`), which is more ceremony than the change's actual footprint justifies | `src/runtime/git.rs` doc comment on `git_bytes`; only one call site (`surface.rs::capture_dirty_patch`) was migrated in this commit | low | raised | |

### Seat: test-honesty

> Confirmed test #1 genuinely exercises `capture_dirty_patch` → `git_bytes` (correctly `git add -A` then `git diff --cached --binary`). Verified independently that `git apply --check`/`--stat` work correctly outside any git repository (tested in a fresh empty dir: exit 0 for a well-formed patch), so that specific worry in the brief is not itself a flaw. Findings below focus on real gaps.
>
> | id | axis | claim | evidence | severity | status | refutation |
> |---|---|---|---|---|---|---|
> | F-TH-01 | test-honesty | Neither regression test exercises root cause H2 (lossy UTF-8 re-encoding corrupting non-UTF-8 diff bytes), despite it being cited as a second confirmed root cause of #234 | Test #1 fixture content is pure ASCII (`"const V: [u8; 1] = [0];\n"`, `"fn wanted() {}\n"`); test #2's markers are also ASCII text (`validated.txt`/`container-produced-evidence`). `capture_dirty_patch` uses `git diff --cached --binary`, implying binary/non-UTF-8 content is an expected input class, but no fixture in either test contains non-UTF-8 bytes, so the lossy-round-trip failure mode the fix also claims to close is never triggered or pinned | medium | raised | |
> | F-TH-02 | test-honesty | Test #2's new `git apply --stat`/`--check` assertions are not guaranteed to execute — `walk_for_marker` can return `true` via the pre-existing plain-`validated.txt` branch without ever reaching the patch-checking branch, so the test can pass green without the new code path running | The function has two independent match arms that each set `found`: a plain-file arm (untouched by the fix) and a `.patch`-content arm (where the new `git apply` checks live). The brief's only stated guarantee that the dirty/patch path is taken is a comment inference from `completed_dirty` state, not an assertion that specifically the patch arm fired | medium | raised | |
> | F-TH-03 | test-honesty | The "watched failing first" claim (pre-fix `git()`-based capture rejected by `git apply --check` with "corrupt patch at line N") is self-reported by the fix author and not independently verifiable from anything in this brief — no captured pre-fix output, diff, or stage log is shown, only a narrative claim | Status note is explicitly flagged as "self-reported, not independently verified by you"; no artifact (failing CI run, saved stderr, git-log of a revert-and-rerun) is attached | low | raised | |
> | F-TH-04 | test-honesty | The full-suite `cargo test` "10 pre-existing environmental failures, none touching git.rs/surface.rs" claim is asserted, not demonstrated — no test names, no log excerpt, and the causal attribution to ambient `SGT_*` env vars is unverified | Same status note, same self-reported caveat; nothing in the brief lets a reviewer confirm those 10 failures are actually unrelated to the changed files rather than, e.g., masking a regression elsewhere | low | raised | |
> | F-TH-05 | test-honesty | Test #1's assertion that reading the original worktree file returns `Err` is padding unrelated to proving the fix — it verifies teardown reclaimed the directory (already established/assumed test infrastructure behavior), not anything about patch correctness or the newline/UTF-8 bug | The assertion is explicitly commented as "sanity: the worktree must actually be gone by now," disconnected from the patch-validity assertions around it; it neither strengthens nor is required by the `git apply`/content checks that do the real proving | low | raised | |

## Coverage

All four axes ran; none degraded or missing.

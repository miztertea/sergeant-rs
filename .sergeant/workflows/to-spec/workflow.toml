# to-spec — admitted workflow (N1 reference corpus,
# docs/gauntlet/contracts/N1.md, candidate W31). Turns a plan/design into a
# published spec ticket: gathered context, sketched seams, confirmed with
# the user, published on template. Promoted per
# docs/icm/promotion-spec-2026-08-11.md; the full behavior-unit citation
# trail is archived at docs/gauntlet/promoted-provenance/to-spec.md.
#
# Two ordinary actor stages. Stage order here is authoritative; the
# directory listing under this workflow's own tree MUST agree with it
# lexically (docs/icm/convention.md §1 rule 4).

[workflow]
name = "to-spec"
version = "2"
stages = ["00-gather-context", "10-sketch-seams"]

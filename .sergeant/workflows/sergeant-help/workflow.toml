# sergeant-help — admitted workflow (N1 reference corpus,
# docs/gauntlet/contracts/N1.md; candidate W4, reference-corpus/synthesis.md
# §1). Answers a Sergeant usage/setup/troubleshooting question from
# repository-owned documentation with an explicit precedence order,
# read-only, never inventing behavior. Promoted per
# docs/icm/promotion-spec-2026-08-11.md; the full behavior-unit citation
# trail is archived at docs/gauntlet/promoted-provenance/sergeant-help.md.
#
# Three ordinary actor stages (00-classify-and-locate,
# 10-resolve-source-conflicts, 20-answer-or-hand-off) — no engine change.
# Stage order here is authoritative; the directory listing under this
# workflow's own tree MUST agree with it lexically
# (docs/icm/convention.md §1 rule 4).

[workflow]
name = "sergeant-help"
version = "1"
stages = ["00-classify-and-locate", "10-resolve-source-conflicts", "20-answer-or-hand-off"]

# grill-with-docs — admitted workflow (N1 candidate W29,
# docs/gauntlet/contracts/N1.md). Runs the `grilling` interview while using
# the domain-modeling discipline to capture ADRs/glossary entries as
# decisions land — composition without nesting (see ../CONTEXT.md "Notes
# for reviewers"): both stages delegate to `grilling`, inlined ahead of the
# capture step rather than invoked as a true nested workflow (that
# mechanism does not exist yet — docs/icm/convention.md §4). Promoted per
# docs/icm/promotion-spec-2026-08-11.md; full behavior-unit citation trail
# archived at docs/gauntlet/promoted-provenance/grill-with-docs.md.

[workflow]
name = "grill-with-docs"
version = "2"
stages = ["00-interview-loop", "10-confirm-understanding"]

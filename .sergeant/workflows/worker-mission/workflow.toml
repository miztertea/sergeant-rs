# worker-mission — admitted workflow (N1, sergeant-rs-workspace/knowledge/evidence/gauntlet/contracts/N1.md,
# candidate W9). From a rendered brief, produces a merged-ready change
# with evidence — the contract a dispatched worker delivers against.
# Promoted per docs/icm/promotion-spec-2026-08-11.md; full behavior-unit
# citation trail archived at
# sergeant-rs-workspace/knowledge/evidence/gauntlet/promoted-provenance/worker-mission.md.
#
# Four ordinary actor stages (N1 adjudication A4 folded 00-pin-scope
# forward into 10-triage-and-route and 50-publish-result backward into
# 40-escalate-or-continue; see the archived provenance for the full fold
# record). `20-implement` delegates to whichever of diagnose-bug,
# prototype, tdd, implement, or deepen-module was selected at
# 10-triage-and-route (all five already published in this library). Stage
# order here is authoritative; the directory listing under this
# workflow's own tree must agree with it.

[workflow]
name = "worker-mission"
version = "2"
stages = ["10-triage-and-route", "20-implement", "30-independent-review", "40-escalate-or-continue"]

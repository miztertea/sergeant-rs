# cross-repo-work — admitted workflow (N1, sergeant-rs-workspace/knowledge/evidence/gauntlet/contracts/N1.md,
# candidate W7). Decomposes a requested outcome across the repositories
# that own it and defines delivery order before any dispatch happens.
# Promoted per docs/icm/promotion-spec-2026-08-11.md; full behavior-unit
# citation trail archived at
# sergeant-rs-workspace/knowledge/evidence/gauntlet/promoted-provenance/cross-repo-work.md.
#
# Five ordinary actor stages (N1 adjudication A4 folded
# 30-inspect-repository-state into 40-define-delivery-gates as a
# preceding helper invocation; see the archived provenance for the full
# fold record). Stage order here is authoritative; the directory listing
# under this workflow's own tree must agree with it.

[workflow]
name = "cross-repo-work"
version = "2"
stages = ["10-assign-ownership", "20-define-dependency-order", "40-define-delivery-gates", "50-handoff-or-stop", "60-reconcile"]

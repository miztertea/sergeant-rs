# Lessons — moved

This file's content — L1 through L25 — has been decomposed into one
lesson record per entry in the `sergeant-rs-workspace` repo's knowledge
library (ADR 0014 decisions 9 and 17; Phase 4 step 3b of
`sergeant-rs-workspace`'s
`knowledge/evidence/reference/proposal-product-workspace-split.md`, prior
to this file's own migration).

The filename stays here, unlinked from its old content, so that existing
references to `LESSONS.md` (e.g. `docs/DEVELOPMENT.md`) do not 404.

Where the record now lives, in `sergeant-rs-workspace`:

- Lesson records (`L1`–`L25`, one file each) → `knowledge/rulings/lessons/`
- Audit trail for the decomposition, including the still-open question of
  whether lessons keep this file's update-in-place/delete convention or
  move to `rulings/`'s correct-by-new-commit convention (**not decided —
  flagged for an owner ruling**) →
  `knowledge/evidence/decomposition-lessons-environments-2026-08-17.md`

Until that question is ruled on, each lesson record's front matter carries
`discipline: unresolved`.

# Close

Leave the branch in the state a reviewer should find it in: committed, with a
message that explains why rather than what.

Signal stage completion when nothing is left uncommitted that belongs in the
change. The work surface is torn down after this stage; the branch is kept.

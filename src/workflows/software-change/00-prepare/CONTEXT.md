# Prepare

Read the intent and the repository. Establish what "done" means before
changing anything: the files involved, the tests that must pass, and the
constraint you are most likely to violate.

Signal stage completion when you can state the plan in a sentence. Ask for
input instead of guessing when the intent is ambiguous.

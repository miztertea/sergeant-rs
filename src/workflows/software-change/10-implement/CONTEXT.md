# Implement

Make the change on this work surface's branch. The worktree is yours; the
source checkout is not.

Signal stage completion when the change is complete and the tests you named in
prepare pass. Signal failure with the reason rather than leaving the stage
half-done.

# M0 Contract — Bootstrap

**Outcome.** A repository a builder can start real work in: reference corpus
committed, a compiling single-crate scaffold matching proposal §35's layout,
CI enforcing the deterministic gates, ledger and memory files in place, branch
pushed, PR open.

**In scope** (proposal §34–35): crate `sergeant-rs`, binary `sgt`; module
skeleton `src/{main,cli,daemon,api,tui,web}.rs`, `src/domain/`, `src/runtime/`,
`src/backend/`, `src/telemetry.rs` — stubs that compile, no logic. Dependencies:
only what the skeleton needs now (serde, serde_json, clap, tokio, ulid, blake3,
toml, tracing, thiserror); milestone-specific deps arrive with their milestones.
GitHub Actions CI: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`,
`cargo test`.

**Acceptance.** `cargo build && cargo fmt --check && cargo clippy --all-targets
-- -D warnings && cargo test` all green locally; CI workflow file present; PR open
against `main` from `claude/ideaos-agent-prototype-gauntlet-k38as1`.

**Non-goals.** Any runtime behavior. No daemon, no API, no journal logic.

**Unknowns.** None material; M0 is mechanical.

**Gauntlet depth.** Mechanical milestone: deterministic gates + one combined
critic pass (spec-fidelity + simplicity), no full four-axis panel.

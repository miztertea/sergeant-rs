# North Star — orchestrator dispositions over the arbitration

Layer between the synthesis draft (`north-star-draft-2026-08-11.md`) and
the owner's gate. Three challengers argued competing paths
(`north-star-arbitration-2026-08-11.md`); this records what the
orchestrator adopts, overturns, and sends up. The owner rules last.

## Adopted (with the challenger that won it)

- **D1 — E6's premise corrected (experience-first challenger; verified
  against `src/runtime/surface.rs:17-24,190-191,505-512` this session).**
  Teardown always retains the branch and fails closed on dirty worktrees;
  output never evaporates — it becomes invisible. Wave 0's top item
  reframes to the **legibility-and-safety cluster**: an output pointer at
  terminal state (where did my deliverable land), `sgt work transcript`
  (E7), the `promote` disposition actually executing, E1's
  canceled-turn usage capture, E2 surfaced as a product-owned env
  contract, and a submit-time capability preflight warning (E3's honest
  two-line form). Small-to-medium items; no R7 machinery.
- **D2 — the spend envelope ships before any stranger does (core-first
  challenger's demand, adopted unconditionally).** A per-Work hard
  spend/turn envelope enforced at the PREPARE/LAUNCH boundary is the
  answer to the draft's gap 8, sized per L16 (a cap bounds spend only at
  cap + one maximal turn — the contract states that arithmetic). Any
  onboarding wave that lacks it is not ready for strangers by
  definition.
- **D3 — instrument, not deliverable (experience-first challenger's
  fork, taken as synthesis).** AGENTS.md rewritten + minimal `sgt init`
  + the operator-skills layer build in PARALLEL with the legibility
  cluster — they are the apparatus that makes every subsequent
  measurement cheap and self-hosted, and they are mostly content plus
  one verb. What defers is *stranger onboarding*: the prebuilt binary,
  the stranger-facing README, anything that invites judgment — behind
  the spend envelope and a second dogfood round (core-first's
  irreversibility argument: you cannot un-ship a binary; no external
  user exists anywhere in the record to lose).
- **D4 — draft ruling 14 upheld against the orchestrator's own earlier
  R-H0-2 recommendation.** Capability-provenance (contract-v2 item 7) is
  the one item Cerberus measured live; it rides with the legibility
  cluster. Items 1–3+8 wait for R-H0-3's probe like everything else.
- **D5 — upheld from the draft unchanged:** delta 5+8 merge (a finding
  is a queued intent); backlog as a new type with idempotent promotion;
  the NOT-EVER list; R-NS-1..5 boundary rules; dogfood outranks
  pre-dogfood proposals; upstream #52's philosophy rejected at the
  product layer; recursion acceptance test (the first Wave 0 fix runs as
  a sergeant Work against this repo).

## Sent up — the one genuine fork (narrow-and-cut challenger)

**Does sergeant own the interactive conversation, or unattended
execution only?** The only decision in the packet where wrong costs
months versus a week. Unattended-only: E3/G5 demotes to
"unblock: a transport that can express asks, or measured demand from
dogfood round 2"; the WORKFLOW-IF-E3 packages stay honestly gated; the
T-series contract shrinks to the fork-independent slice (legible work
thread + respond + #11/#16 — `needs_input`/`respond` already ship).
Interactive-owned: E3's re-enterable hold builds now, T-series's
attention model proceeds, roughly half of H-series follows.
**Orchestrator recommendation: unattended-first.** The asymmetry
decides it — the ask affordance this rests on was deleted by the vendor
in an undocumented patch release (measured, five probes), and the
recovery path if we're wrong is one dogfood round plus machinery that
already exists. The challenger's framing is adopted verbatim as the
question; the owner rules.

## Effect on the roadmap if the owner takes the recommendation

Wave 0 = legibility-and-safety cluster + spend envelope + blocked
exit-door test, **with the instrument (AGENTS.md/init/skills) built in
parallel** and the recursion test as the wave's own acceptance gate.
Wave 1 = estate (repos/ manifest, data-dir flip, per-repo contract,
E5); backlog/queued intents follow; surfaces stay minimal;
N4/H1/T-full/prebuilt-binary all hold their unblock conditions.

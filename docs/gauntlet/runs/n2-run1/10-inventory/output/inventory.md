# AMBIGUOUS — NOT RESOLVED

Relayed from the upstream artifact named in this stage's own Inputs table,
`../00-contract/output/contract.md`, which itself opens with this same
heading. Per `../_config/run-discipline.md` §2, this stage does not proceed
with its ordinary work (enumerating files, assigning dispositions,
partitioning `decompose` rows) — none of that is possible without a
resolved subject repository, pinned revision, and scope, which `00-contract`
was unable to establish. This document is the mechanical propagation of
that unresolved state, not a re-diagnosis.

What is ambiguous, quoted from `00-contract`'s "What is ambiguous" line:

> What is ambiguous: the pinned revision of this run's subject cannot be
> established, and the subject identification itself rests on inference
> rather than an explicit statement in the Work's initiating task.

No inventory (file enumeration, dispositions, partitions, or the required
disposition/partition count summary) is produced in place of the above —
per `../_config/run-discipline.md` §2, this is the fail-closed marker
itself, not a partial inventory for `20-harvest` to build on.

See `../00-contract/output/contract.md` in full for the complete diagnosis
("What was checked" list and the meta-level grammar-pressure note) that
this stage is relaying rather than repeating.

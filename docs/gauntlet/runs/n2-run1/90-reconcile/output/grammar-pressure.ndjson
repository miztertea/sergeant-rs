# AMBIGUOUS — NOT RESOLVED

Relayed from the upstream artifacts named in this stage's own Inputs
table — `../00-contract/output/contract.md` through `../70-lint/output/
lint-report.md`, all eight of which open with this same heading, and
specifically `../40-classify/output/classifications.ndjson`, the direct
source of every behavior-level grammar-pressure record this file would
otherwise consolidate. Per `../_config/run-discipline.md` §2 and this
stage's own `CONTEXT.md` step 0, this stage does not perform the ordinary
`references/reconciliation-method.md` §3 consolidation. This document is
the mechanical propagation of that unresolved state, not a re-diagnosis,
and is deliberately not valid NDJSON on this run — there is no record to
emit.

What is ambiguous, quoted from `00-contract/output/contract.md`'s "What is
ambiguous" line (relayed unchanged through every intermediate stage):

> What is ambiguous: the pinned revision of this run's subject cannot be
> established, and the subject identification itself rests on inference
> rather than an explicit statement in the Work's initiating task.

## Behavior-level: none, by construction

`../40-classify/output/classifications.ndjson` contains zero
classification records (only the AMBIGUOUS relay prose) — there is no
`representation: engine-gap` record anywhere in this run for this file's
behavior-level side to carry forward.

## Meta-level: one genuine moment recorded upstream, not consolidated here

`../00-contract/output/contract.md` records, under its own "Meta-level
grammar-pressure moment" heading, that the current engine gives no actor
stage a way to pause its own turn and wait for a human's disambiguating
answer mid-run, and states explicitly that it is "[r]ecorded here per
`../_config/run-discipline.md` and `90-reconcile/references/
reconciliation-method.md` §3 for `90-reconcile` to pick up." That moment
is real and was genuinely recorded, not invented — but this stage does not
convert it into a full six-field `engine_gap` record here: doing so would
require this stage to originate the mapping from `00-contract`'s free-form
account onto the template's `lower_rungs_attempted` field (which of
ICM-ladder rungs 6.1–6.6 were considered, and why each fails, stated
individually per rung) and `why_each_fails`, neither of which
`00-contract`'s note states in those terms. Constructing that mapping now
would be "originating a new explanation" rather than relaying one already
recorded (`../_config/run-discipline.md` §2, which requires quoting, not
re-deriving) — exactly the ordinary, judgment-requiring work this stage's
own `CONTEXT.md` step 0 forecloses once any Inputs-table artifact carries
the marker. The moment remains genuine and available for consolidation by
a future, resolved run of this stage: see `../00-contract/output/
contract.md`'s "Meta-level grammar-pressure moment" section in full.

## Step 4 (closing the run) also not executed

Per this stage's own `CONTEXT.md` step 0, `../scripts/finalize.py` was not
run this pass. See `output/measurement-package.md`'s own note on this for
the full explanation — recorded there once rather than repeated per file.

# AMBIGUOUS — NOT RESOLVED

Relayed from the upstream artifacts named in this stage's own Inputs
table — `../00-contract/output/contract.md` through `../70-lint/output/
lint-report.md`, all eight of which open with this same heading. Per
`../_config/run-discipline.md` §2 and this stage's own `CONTEXT.md` step 0,
this stage does not assemble the ordinary `references/reconciliation-
method.md` §2 measurement package. Every stage that would supply a real
value for any of proposal §9.9's five internally-computable dimensions
(source coverage, behavioral precision, provenance completeness, draft
validity, review convergence) is itself this same unresolved marker, not
real data. Reporting "0 files," "0/0 citations," or "N/A" for those
dimensions would misrepresent an absent corpus as a measured one — exactly
the "measuring against artifacts you know are hollow" this stage's
`CONTEXT.md` step 0 instructs against. This document is the mechanical
propagation of that unresolved state, not a re-diagnosis, and not an
attempt to fill the gap with placeholder numbers.

What is ambiguous, quoted from `00-contract/output/contract.md`'s "What is
ambiguous" line (relayed unchanged through every intermediate stage):

> What is ambiguous: the pinned revision of this run's subject cannot be
> established, and the subject identification itself rests on inference
> rather than an explicit statement in the Work's initiating task.

## The ten §9.9 dimensions, named for completeness

`references/reconciliation-method.md` §2 states all ten by name so
recovering them never requires opening the proposal document. None is
reported below with a value — not the five ordinarily internally
computable, and not the five that require `reference-corpus/` comparison —
because this run never produced the corpus any of them would measure:

```text
INTERNALLY COMPUTABLE (ordinarily, on a resolved run — not reported here):
source coverage, behavioral precision, provenance completeness,
draft validity, review convergence.

NOT COVERED BY ANY run of this stage (require reference-corpus/ comparison
by independent, later comparers not bound by this run's own blindness
rule, docs/gauntlet/contracts/N2.md Outcome §3):
behavioral recall, workflow-boundary agreement, stage-boundary agreement,
representation agreement, engine-gap quality.

SUPPORTING RUN STATISTICS (ordinarily reported alongside, not §9.9
dimensions themselves — not reported here):
run identity, extraction coverage, normalization outcome,
representation mix, candidate yield, draft materialization.
```

Each of the internally-computable and supporting-statistics rows above
would ordinarily draw from a named upstream stage (`00-contract` through
`60-draft`); every one of those stages is itself the unresolved marker on
this run, so there is no honest value to put beside any row.

## Step 4 (closing the run) also not executed

Per this stage's own `CONTEXT.md` step 0 ("do not proceed with steps
1–4"), `../scripts/finalize.py` was not run on this pass, so no
`## Finalize` section with its verbatim output appears at the end of this
document as `references/reconciliation-method.md` §4 would otherwise
require. This is recorded here, rather than left silent, so a later
resolved run of this stage does not have to infer why the section is
missing: it is missing because the AMBIGUOUS short-circuit fired before
this stage reached step 4, not because the closing act was overlooked.
`../scripts/finalize.py`'s own disposition logic is orthogonal to subject/
revision resolution (it reads only each stage's `output/README.md`), so
running it would not itself require inventing facts about the unresolved
subject — but this stage's own `CONTEXT.md` step 0 names it as one of
"steps 1–4" not to proceed with, without carving out an exception for its
mechanical nature, so it is treated the same as steps 1–3 here rather than
selectively executed.

See `../00-contract/output/contract.md` in full for the complete diagnosis
("What was checked" list and the meta-level grammar-pressure note) that
every stage in this chain, including this one, relays rather than repeats.

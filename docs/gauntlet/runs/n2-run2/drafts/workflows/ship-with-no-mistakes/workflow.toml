# ship-with-no-mistakes — DRAFT candidate, materialized by repo-to-icm's
# 60-draft stage. Not admitted; do not load into the runnable engine
# (docs/icm/convention.md §2).
#
# stages = [] is deliberate, not an omission: this corpus classifies zero
# `stage` records under this workflow value — see CONTEXT.md and
# provenance.md. A workflow.toml with an empty stages list cannot be
# loaded by the runnable engine, which is consistent with this package's
# draft status; a human reviewer must define real stages (or reclassify
# this candidate) before promotion.

[workflow]
name = "ship-with-no-mistakes"
version = "1"
stages = []

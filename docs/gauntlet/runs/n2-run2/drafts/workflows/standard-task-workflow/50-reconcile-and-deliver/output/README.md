# Output — `50-reconcile-and-deliver`

Layer 4 (per-run artifact), per `docs/icm/convention.md` §1a. Empty at
draft time — this describes artifact shape for **this candidate's own
future runs** once promoted, not an artifact of the `repo-to-icm` run that
materialized this draft package.

**Expected artifact:** a delivery record — the verified terminal state and
preserved evidence checked before cleanup, and the cleanup outcome itself.

**Disposition:** `promote`

This is the workflow's own closing record — the evidence that cleanup did
not run ahead of verification.

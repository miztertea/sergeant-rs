# standard-task-workflow — DRAFT candidate, materialized by repo-to-icm's
# 60-draft stage. Not admitted; do not load into the runnable engine
# (docs/icm/convention.md §2).

[workflow]
name = "standard-task-workflow"
version = "1"
stages = [
    "10-load-context",
    "20-check-queue",
    "30-reconcile-existing-state",
    "40-validate",
    "50-reconcile-and-deliver",
]

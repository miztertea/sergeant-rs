# 02-review-implementation

## Inputs

| File | Layer | Why |
|---|---|---|
| ../01-implement-at-seams/output/outcome.md | L4 | upstream evidence produced by `implement-at-seams` — this stage's own future runs expect that checkpoint to have already been reached |

## Purpose

**Trigger:** implementation work is complete

**Outcome:** the finished work receives a code review before being considered done

**Statement (the operative rule):** /code-review is used to review the completed implementation work.

## What must become true here (durable outcome)

The finished work receives a code review before being considered done — per the Statement above, which is the operative rule this stage exists to enforce.


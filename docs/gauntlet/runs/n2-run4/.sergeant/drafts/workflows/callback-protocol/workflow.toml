# callback-protocol — draft workflow candidate materialized by repo-to-icm's
# 60-draft stage (N2 run, 21-partition corpus). status: draft in
# index.md; this package has not been promoted to .sergeant/workflows/.
# See provenance.md for the behavior_id evidence backing each stage.

[workflow]
name = "callback-protocol"
version = "1"
stages = ["01-resolve-callback-executable", "02-register-origin", "03-sync-and-produce-events", "04-enqueue-event", "05-invoke-consumer", "06-process-acknowledgement", "07-retry-delivery"]

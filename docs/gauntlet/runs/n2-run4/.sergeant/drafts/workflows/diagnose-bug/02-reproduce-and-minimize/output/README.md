# Output — `02-reproduce-and-minimize`

Layer 4 (per-run artifact) for **this candidate's own future runs** — empty now, per `.sergeant/workflows/repo-to-icm/60-draft/references/draft-package-template.md` ("What never happens in a draft package": no populated file besides this README under any `NN-.../output/`).

**Expected artifact:** `outcome.md` — a short per-run record of this stage's own outcome (what was validated, written, or decided when this stage executed for a given Work), for the next stage or an operator to consult.

**Disposition:** `evidence`

# provenance — wayfinder

Maps every stage (and the workflow as a whole) to the `behavior_id`(s) from `.sergeant/workflows/repo-to-icm/40-classify/output/classifications.ndjson` that justify it, per `.sergeant/workflows/repo-to-icm/60-draft/references/draft-package-template.md`. A stage with no direct source evidence is marked as a justified design inference with a one-line reason, never left silent and never given an invented citation.

## Workflow as a whole

- Evidenced by this candidate's member stages' own citations below (no single record states the workflow-as-a-whole; the aggregate is the union of its stages, per `.sergeant/workflows/repo-to-icm/50-synthesize/output/candidates.md`).

**Workflow-level helpers** (`stage=null`, apply throughout):

- `BU-1004`

## Stages

### `01-select-wayfinder-mode`

- Primary behavior_id: `BU-1000` (`.agents/skills/wayfinder/SKILL.md (.agents/skills/wayfinder/SKILL.md L13-13)`)

### `02-claim-ticket`

- Primary behavior_id: `BU-1007` (`.agents/skills/wayfinder/SKILL.md (.agents/skills/wayfinder/SKILL.md L67-67)`)

### `03-chart-the-map`

- Primary behavior_id: `BU-1022` (`.agents/skills/wayfinder/SKILL.md (.agents/skills/wayfinder/SKILL.md L111-111)`)
- Stage-context attachments: `BU-1023`, `BU-1024`, `BU-1025`, `BU-1026`, `BU-1027`

### `04-work-through-map-session`

- Primary behavior_id: `BU-1028` (`.agents/skills/wayfinder/SKILL.md (.agents/skills/wayfinder/SKILL.md L122-123)`)
- Stage-context attachments: `BU-1009`, `BU-1011`, `BU-1012`, `BU-1013`, `BU-1014`, `BU-1016`, `BU-1017`, `BU-1018`, `BU-1019`, `BU-1020`, `BU-1021`, `BU-1029`, `BU-1030`, `BU-1031`, `BU-1032`


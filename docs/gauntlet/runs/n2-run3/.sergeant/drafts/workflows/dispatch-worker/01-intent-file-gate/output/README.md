# Output -- `01-intent-file-gate`

Layer 4 (per-run artifact), for this candidate's *own future runs* -- not this `repo-to-icm` run's artifact, and not populated here (`../../../../../workflows/repo-to-icm/60-draft/references/draft-package-template.md` "What never happens"; `docs/icm/convention.md` §1a rule 4).

**Expected artifact (draft inference from `BU-0140`, subject to promotion review):** a durable record of this stage's outcome -- Gates any mutating dispatch action behind a validated intent file when the objective touches a sensitive category -- the earliest possible checkpoint, since it must run before task creation or worker spawn.

**Disposition:** `promote` -- this stage's record is consumed directly by the next stage in this candidate's own chain.

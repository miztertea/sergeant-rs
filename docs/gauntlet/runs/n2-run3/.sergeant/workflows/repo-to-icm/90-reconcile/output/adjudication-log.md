# Adjudication log — `90-reconcile`

Per `references/reconciliation-method.md` §1. Disposes every finding in
`../80-adversarial-review/output/findings.ndjson` (AF-0001 through AF-0005,
the entire population — nothing was sampled). Step 0 check: none of the
Inputs-table artifacts open with `# AMBIGUOUS — NOT RESOLVED` (verified
directly against `00-contract`, `10-inventory`, `20-harvest`'s two
artifacts, `50-synthesize`, `60-draft`, `70-lint`, `80-adversarial-review`'s
`review-summary.md` — see `output/measurement-package.md` for the full
per-artifact check), so ordinary adjudication proceeded per
`../_config/run-discipline.md` §2.

| Finding | Disposition | Reason |
|---|---|---|
| AF-0001 | park | Correct and severe: 15 of 21 partitions (`../20-harvest/output/partition-ledger.md`) are still `pending`. Re-verified directly: rows P7–P21 all show `Status: pending`, blank `Unit id range`/`Notes`. No repair available to `90-reconcile` — resolving it means actually harvesting the 54 unreached files, which requires re-running `20-harvest` (and everything downstream of it), not editing an already-written artifact. What would resolve it: re-run `20-harvest` to complete partitions P7–P21, then re-run `30-normalize` through `70-lint` over the expanded corpus. |
| AF-0002 | park | Correct, same root cause as AF-0001. Re-verified directly: `consequence-class-sweep.md` has exactly 28 data rows (counted), one per P1–P6 file, no row at all (not even a `swept, none found` row) for any of the 54 P7–P21 files. Not repairable by `90-reconcile` for the same reason as AF-0001 — the sweep itself requires reading and judging each of the 54 unswept files, which is `20-harvest`'s own work, not an edit to existing content. What would resolve it: same re-harvest as AF-0001, extending the sweep to the newly-reached files. |
| AF-0003 | accept (partial) | Correct: none of `30-normalize`, `40-classify`, `50-synthesize`, `60-draft` disclosed that the 312-record corpus underlying them covers only 28 of 82 scoped files. Re-verified directly: `python3 -c "len({json.loads(l)['source']['path'] for l in open('20-harvest/output/behavior-units.ndjson')})"` = 28; `wc -l` on all three ndjson ledgers = 312. Repair applied: added a "Coverage caveat" note to `../50-synthesize/output/candidates.md` (after the Accounting paragraph) and to `../60-draft/output/draft-report.md` (after its opening paragraph), each stating the 28/82-file, 6/21-partition coverage plainly and pointing to this adjudication and the measurement package. The other two named targets — `../30-normalize/output/behavior-units.normalized.ndjson` and `../40-classify/output/classifications.ndjson` — were **not** edited: both are declared one-record-per-line NDJSON ledgers with no field for a document-level caveat (`output/README.md` for each names the declared record shape verbatim), and inserting a non-conforming line would itself violate that shape rather than fix a gap. Their incompleteness is disclosed instead via the two prose files above and this log. |
| AF-0004 | accept | Correct: BU-0039, BU-0146, BU-0159, BU-0174, BU-0286 (all `stage-context`, `workflow: worker-response-and-recovery`, `stage: recover-worker`) each justified their §6.3 "no" by claiming the checkpoint is "established more specifically elsewhere" — re-verified directly (`grep '"stage": "recover-worker"' classifications.ndjson` combined with `"representation": "stage"` = zero matches): no `representation: stage` record for `recover-worker` exists anywhere in the 312-record corpus, so the chain of five mutual references never terminates in an actual stage record. That is circular, not merely unattached. Repair applied: rewrote all five records' `rationale` in `../40-classify/output/classifications.ndjson` to state plainly that no attached stage record exists (naming this finding) instead of claiming one is established elsewhere, and to name the promotion question explicitly as open and unresolved by this pass. **Not** applied: promoting any of the five to `representation: stage` (which the finding's own closing sentence suggests may be the more honest answer). That would require re-running `50-synthesize`/`60-draft` to materialize a third stage directory in the already-validated `worker-response-and-recovery` package (`../70-lint/output/lint-report.md` validated the 2-stage shape; `../80-adversarial-review` reviewed that same shape) — restructuring a package after its own validation/review pass already ran is outside `90-reconcile`'s mandate (this stage adjudicates and repairs content in place; it does not re-trigger upstream stages). Recorded as an open classification-boundary question for a follow-up run or dedicated promotion review, not silently resolved either way. |
| AF-0005 | park | Correct: `../50-synthesize/output/candidates.md`'s Bucket 6 ("None. Zero classification records ... carry an `obsolete-mechanism` disposition ... checked directly against the full 312-record ledger") justifies the empty tier by count alone, unlike the per-unit reimplementation-test reasoning given for other tiers in the same document. Re-verified: `../50-synthesize/output/candidates.md`'s own Bucket 1-3 and Bucket 7 sections give named per-record reasoning; its Bucket 6 section gives none. Not repairable by `90-reconcile`: the fix is to actually examine candidate units against the obsolete-mechanism question and record why each doesn't clear it (or does) — that is new judgment/analysis `50-synthesize` itself would have to perform against the source corpus, not a correction to an already-recorded fact. Inventing that reasoning here would be exactly the kind of unevidenced judgment this run's discipline forbids. What would resolve it: `50-synthesize` re-running its own Unused-tiers check (`references/challenge-checklist.md` Axis 4 §3) with explicit per-candidate-unit reasoning, not a bucket count. |

## Summary

5 findings adjudicated: 1 accept (AF-0004, rationale corrected in place), 1
accept-partial (AF-0003, caveat added to the 2 of 4 named targets capable of
carrying one), 0 reject, 3 park (AF-0001, AF-0002, AF-0005 — all real,
correct findings whose actual resolution requires re-running an upstream
stage or performing new judgment this stage is not positioned to invent).
No finding was rejected — every one of the 5 held up on independent
re-verification.

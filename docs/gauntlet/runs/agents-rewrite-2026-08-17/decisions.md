# AGENTS.md rewrite — decision log

Per ADR 0015 decision 4: every PR whose content exceeds a single
mechanical change carries a decision log, one row per material choice,
each tracing to a ruling or marked undelegated — parked.

Scope: `AGENTS.md` rewrite as Layer-0 doctrine, creation of
`.sergeant/common/contexts/ponytail.md`, re-homing of
`.sergeant/common/contexts/bounded-judgment.md`. Read-first sources:
`docs/icm/convention.md` §§3–4, `docs/adr/0014-...md`,
`docs/adr/0015-...md`, `reference/proposal-product-workspace-split.md`
§§4.1–4.7, `.sergeant/common/contexts/bounded-judgment.md`,
`reference/notes/ideaos-agent-contract.md`, prior `AGENTS.md`. The
skew-check findings file named in the brief
(`docs/gauntlet/runs/skew-check-2026-08-17/findings.md`) does not exist in
this checkout — noted below as its own row, not silently skipped.

| Decision | Rung | Evidence | Resolution |
|---|---|---|---|
| Remove the entire "Standard workflow loop" section (all eight steps) | J4 | ADR 0014 consequences: "`AGENTS.md` loses its 'Standard workflow loop' section: eight numbered steps of Layer-1 CLI mechanics occupying a Layer-0 file." Proposal §4.1. | Applied — section deleted, not trimmed. |
| Remove every `BU-####` citation and the closing 126-unit-corpus paragraph | J4 | Proposal §4.1 Phase-1 bullet: "all BU citations removed." Task instruction. | Applied. Confirmed 0 remaining `BU-` occurrences. |
| Both ladders (CONSTRUCTION R1–R7, AUTHORITY J5–J0 + PACE) ship inline in `AGENTS.md`, in full rung text | J4 | ADR 0014 decision 10. | Applied. |
| Ponytail's "what minimality does not mean" clause included verbatim in substance | J4 | Proposal §4.3; `reference/notes/ideaos-agent-contract.md`. | Applied. |
| PACE and succession of authority added below J0, framed as routes to an authority, never decision latitude | J4 | ADR 0014 decision 14 (amendment, corrected framing after SPLIT-1). | Applied with the corrected framing, not the original SPLIT-1-flagged one. |
| ROUTING ladder (dispatch vs. in-session) restated in the same table+rule shape as CONSTRUCTION/AUTHORITY, replacing "When NOT to use `sgt`" prose | J4 | Proposal §4.3: "Routing ladder — currently prose... Restated in the same shape as the other two." | Applied. |
| CAN and SHOULD written as two separately labelled registers; no SHOULD sentence implies enforcement | J4 | ADR 0014 §4.2 / proposal §4.2. | Applied as `## SHOULD` (ladders, routing table, guardrails-as-norms) and `## CAN` (code-verified only). |
| CAN register limited to: daemon single-owner data-dir lock, submit-time capability preflight, worktree isolation, manifest/version refusal, `sgt doctor` named-remedy fail-closed | J1 (verification of existing enforced behavior, not a new grant) | Verified in source: `src/daemon.rs` (`daemon.lock`, `DaemonError::Locked`); `src/runtime/engine.rs::bind_stages` ("the whole-workflow capability preflight"); `src/runtime/surface.rs` (one worktree per repo per Work); `src/domain/manifest.rs` (refused edits); `src/backend/claude.rs:1573` (unmeasured-CLI refusal); `sgt doctor --help` ("Every failing check names the remedy"). | Applied. Two prior guardrail claims ("`sgt init` never writes to `AGENTS.md`/`CLAUDE.md`"; the td/Graphify/Treehouse confirmation prompt) had no equivalent code evidence found by grep — placed in SHOULD, not CAN, per the task's default-to-SHOULD-if-unverified rule. |
| `bounded-judgment.md` re-homed: J5–J0 rung text removed and pointed at `AGENTS.md`; file reduced to the stage-specialization contract, decision-evidence shape, conflict rule, authority-inheritance diagram, and worked example | **J4 — resolved 2026-08-17, was mis-rung as J0** | `docs/icm/convention.md` §3 rule 2 (AGENTS.md "MUST NOT duplicate content that already lives in... a shared context") directly conflicts with ADR 0014 decision 10 ("both ladders ship inline"). `bounded-judgment.md` itself is ratified by ADR 0013 decision 1 — editing what it contains is a material change to a ratified artifact, which ADR 0013 decision 1 does not by itself authorize this Work to make. | Implemented per the task brief's explicit "Captain's resolution": `AGENTS.md` canonical for rung text, `bounded-judgment.md` re-homed to what only it needs. This is a re-homing, not a duplication, but it remains **undelegated by any ADR this Work can cite on its own authority** — recorded as an open J0 in the commit message per instruction, not silently resolved. **Corrected, same day.** This was escalated as J0 on the reasoning that two governing constraints conflicted. That was a mis-application of the ladder: J4 is "the user... already decides the question" and ADR 0014 decision 10 does decide it. What was actually missing was not a ruling but a *supersession note* — `docs/adr/README.md` requires a later decision to say it supersedes an earlier one rather than leaving the two in silent contradiction. `docs/icm/convention.md` §3 and §6 now record it, and canonicity moved rather than being copied, so rule 2's purpose (one source, no drift) is preserved. Escalating this cost the owner a round-trip that the repository's own supersession convention already answered. |
| `.sergeant/common/contexts/ponytail.md` created and reduced the same way (no R1–R7 table; only the rung-logging convention and a boundary note), rather than containing a full copy of the ladder | undelegated — parked | Task instruction item 4 requires the file to exist so `@@ponytail` resolves. No ADR rules on Ponytail's shared-context content the way ADR 0013 rules on `bounded-judgment.md`, so the anti-duplication vs. inline-ladder conflict here has lower stakes than the row above, but the same tension exists between convention.md §3 rule 2 and ADR 0014 decision 10. | Resolved by analogy to the bounded-judgment resolution above (same shape of conflict, same fix), not by an independent ruling. Recommendation: owner confirms this extension covers Ponytail too, or rules it should instead carry the full table with `AGENTS.md` deferring to it (the inverse of what's implemented). |
| Opening description changed from "a cloned directory of instructions, skills, and conventions" to "embedded in the `sgt` binary and written to disk by `sgt init`"; "whether or not anyone is watching" dropped | J5 | ADR 0014 decision 1 (distro delivery — embedded, not a cloned directory) directly contradicts the prior text. Decision 8 struck "whether or not anyone is watching" from `NORTH-STAR.md` for the same reason (consequence, not thesis) — `AGENTS.md`'s own echo of that phrase is the same defect. | Applied. |
| Narrated workflow count ("20 published workflows at last count") removed from the trigger-table framing paragraph | J1 | Proposal §2.1 names narrated counts as an instruction-fiction pattern (`.sergeant/index.md`'s own count); the acceptance criteria push toward computed, not narrated, counts generally. Not itself ADR-ruled for this specific sentence. | Local, reversible wording fix — resolved directly rather than parked; nothing routes through this count today. |
| "Working on sergeant-rs itself" trimmed from a full paragraph to a single pointer sentence | J4 | Task instruction 1: "gains... a single pointer to `docs/DEVELOPMENT.md`." | Applied. |
| `docs/DEVELOPMENT.md` absorption: nothing added | J1 | Reviewed every removed block. Standard-workflow-loop content is Layer-1 CLI mechanics about *operating* an estate (superseded by `sgt --help`, not development-of-sergeant-rs content) — proposal §4.1 says the binary documents its own surface, not that `DEVELOPMENT.md` should. The removed BU-citation/126-unit closing paragraph duplicated `DEVELOPMENT.md`'s own existing "Session conduct on this repo" section almost exactly. | No content moved. Recorded explicitly so the omission reads as a checked finding, not a skipped step. |
| `docs/icm/convention.md` §6 still calls `.sergeant/common/contexts/bounded-judgment.md` "canonical source" for J5–J0 | undelegated — parked | Now stale relative to the re-homing above; convention.md itself is out of this task's stated scope (not in the "what to produce" list) and is binding doctrine this Work has no ruling authorizing it to edit. | Not touched. Flagged for the owner alongside the `bounded-judgment.md` ruling — the two should be settled together. |
| `docs/gauntlet/runs/skew-check-2026-08-17/findings.md`, named in the brief as required reading, does not exist in this checkout | undelegated — parked | Read-first instruction named this file explicitly; a repo-wide search found no file matching `*skew*` under `docs/gauntlet/runs/`. | Proceeded without it — cross-checked all CLI-surface claims directly against `sgt --help` output instead (no flag names were kept in the final `AGENTS.md`, since the whole CLI-mechanics section was removed rather than restated). Recommend the owner confirm whether the findings file was supposed to exist yet, or whether the skew-check Work referenced in the brief hasn't landed. |

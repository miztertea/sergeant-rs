# Output — `10-touch`

Layer 4 (per-run artifact). This directory is empty in the authored tree; a
run of this stage edits `notes/status.md` in place (not a new file here) —
the edit itself, visible in the Work branch's diff, is this stage's
evidence.

**Expected artifact:** the updated `notes/status.md` (round counter bumped,
one observation appended).

**Disposition:** `evidence`

# Cerberus (owner's home server, cerberus.what-it.be on the local network)

Facts measured 2026-08-11, first session on this host (N-series close-out
handoff). Persistent host — no container-reset hazard — but facts still
drift on OS/tool updates; re-measure on suspicion and date any change.

| Fact | Measured value | Evidence |
|---|---|---|
| uid | 1001 (`miztertea`), groups include `sudo`, `docker` | `id`, 2026-08-11 session probe |
| DAC / permission-bit fixtures | **enforced** (chmod 000 read fails EACCES) — opposite of the root container; permission-bit fault fixtures work here | session probe 2026-08-11 |
| `CAP_LINUX_IMMUTABLE` | not available without sudo (`chattr +i` → EPERM); same shape as the GH runner | session probe 2026-08-11 |
| `IS_SANDBOX=1` | **not required** (exists only to work around the root refusal) | gate.sh portability commit c585589 analysis |
| Disk | 935 GB LVM/ext4 root, ~864 GB free at first contact; `quota`/`repquota` absent, `xfs_quota` present (xfsprogs ships it regardless of fs; root fs is ext4 so it is inert here) | `df -h` + probe-env cross-check, 2026-08-11 |
| Cores / kernel | 20 cores; Linux 7.0.0-29-generic | `nproc`, `uname -r`, 2026-08-11 |
| /tmp | tmpfs, 16 GB; **O_DIRECT open SUCCEEDS on tmpfs here** (kernel 7.0) — differs from the cloud container where tmpfs refused it | python O_DIRECT probe, 2026-08-11 |
| O_DIRECT on ext4 (home fs) | open succeeds | python O_DIRECT probe, 2026-08-11 |
| Outbound network | open, no proxy vars; GitHub release/raw fetches return 200 (the cloud container's 403 does not apply — release installers work here) | curl probes, 2026-08-11 |
| Docker | 29.7.2, storage driver **overlayfs**, cgroup v2 (systemd driver), user in `docker` group (no sudo needed). Full lifecycle measured: registry pull (`alpine`), **digest-pinned pull**, bind-mount read+write, `--network=none`, bridge egress to 1.1.1.1, image removal — all green. §22.7's matrix is runnable on this host | docker lifecycle + digest probes, 2026-08-11 session |
| Docker container-written file/directory ownership (§16.8, N4's named Unknown) | Without `--user`, a container runs as the image default — **root** in every measured probe image — and anything it writes into a `read_write` mount lands root-owned on the host (uid 1001, docker group). Measured **stronger than the ownership-only claim**: a root-owned *directory* the container creates cannot be `rm -rf`'d by the host user either, breaking worktree teardown, not just editing. **Worktree contract (fixed, MVP-2 D3 fixer pass, INV-R1-04/TH-11):** `DockerBackend::create_container` now passes `--user <uid>:<gid>` sourced from the mounted worktree directory's own host owner (`std::fs::metadata` via `MetadataExt`, unix-only) — no new identity invented, the worktree's existing owner is carried into the container. Verified post-fix: both a container-written file and a container-created directory come out owned by the host user, and are removable/editable by that user afterward. | `docker run --mount type=bind,... alpine:3 sh -c 'echo hi > /w/f; mkdir /w/d'` pre-fix → `-rw-r--r-- 1 0 0 f`, `drwxr-xr-x 2 0 0 d`; `tests/m7_docker_executor.rs::container_written_files_and_directories_are_owned_by_the_host_worktree_owner`, 2026-08-12 fixer-pass session |
| Installed `sgt` | `~/.cargo/bin/sgt` exists (owner-installed). A test daemon of it ran at first contact (pid 35846, data dir `~/.local/share/sergeant`); owner-directed cleanup same day — stopped, data dir removed. Test leak checks pattern on `debug/sgt`/`release/sgt` paths and cannot collide with an installed-binary daemon; recheck `pgrep -af "cargo/bin/[s]gt"` on wake | owner direction + bracketed pgrep, 2026-08-11 |
| Claude CLI | v2.1.227 at `~/.local/bin/claude`; opt-in m4 suite re-measured 2026-08-11: a1/a3 green, **a5 red — `post_turn_summary` absent on this host** (5-probe isolation: all models/modes/tool-use → 0 lines; GP-2's ask affordance gone, runtime withdrawal path operative). Auth method `claude.ai` (container was `oauth_token`/firstParty) — plausible gating variable, not isolated | `docs/gauntlet/notes/cerberus-ask-grammar-remeasurement-2026-08-11.md` |
| `claude --dangerously-skip-permissions` under uid 1001 | **viable** — a5/a1/a3 drove real turns with no refusal, when this ran. **Correction, 2026-08-11 (round-2 finding INV-R2-09):** the parenthetical "(which carries the flag)" is now false at HEAD — #47 (`06fb6e8`) made permission mode profile config and removed the flag from `REQUIRED_FLAGS`/the launch grammar entirely; `None`/default profile now emits no permission flag at all (`src/domain/profile.rs`, `PermissionMode::Default::cli_args()` empty). The uid-1001 viability fact itself is untouched — only the claim about what the adapter's launch grammar carries. | opt-in m4 suite run, 2026-08-11; correction verified against `src/backend/claude.rs` `REQUIRED_FLAGS` and `src/domain/profile.rs`, 2026-08-11 |
| no-mistakes | preinstalled at `~/.local/bin/no-mistakes`, runs under a systemd --user unit (not a direct fork — inline env prefixes are discarded; gate.sh detects and handles this) | c585589 commit message |
| Rust toolchain | cargo/rustc 1.97.1 in `~/.cargo/bin` — **not on non-interactive shells' default PATH**; prefix `PATH="$HOME/.cargo/bin:$PATH"` in scripts | `which cargo` miss + explicit path check, 2026-08-11 |
| Build deps | gcc/g++/make/pkg-config present; `cmake` absent (bundled DuckDB builds without it — warm build verified) | `which` probe + green `cargo build`, 2026-08-11 |
| Claude CLI version drift | v2.1.228 at `~/.local/bin/claude` as of the MVP-2 D2 session, one patch ahead of this file's 2.1.227 row above (dated 2026-08-11) — recorded per L14's drift-is-a-fact rule, not chased further; `MIN_TRUSTED_VERSION` stays 2.1.226 | `claude --version`, 2026-08-12 |
| `--setting-sources` scope | governs `.claude/settings*.json`-style configuration only (permissions, hooks, MCP config) — **not** whether the actor's agentic loop reads a memory file already in its cwd. Native "CLAUDE.md auto-discovery" (the CLI's own `--bare --help` term) is a separate, always-on mechanism keyed to the literal filename `CLAUDE.md`, identical under no flag / `--setting-sources user` / `--setting-sources user,project`; it never fires for a file named `AGENTS.md` (Sergeant's instruction filename) under any of those. Full measurement, methodology, and the two design consequences: `docs/gauntlet/notes/d2-setting-sources-measurement-2026-08-12.md` | 5 bounded `claude-haiku-4-5` turns against a scratch repo, 2026-08-12 |
| Interrupt: SIGKILL vs SIGTERM | `ClaudeBackend::interrupt`/`stop` send SIGKILL (`Child::kill()`): no terminal `type:"result"` envelope ever, confirmed again; whatever `assistant` chunks streamed before the kill remain in the raw archive and may carry a per-chunk (not cumulative) `usage` object. SIGTERM — not a mechanism this adapter uses — is measured to behave differently: the CLI traps it, aborts gracefully, and emits a terminal `result` envelope (`is_error:true`, `terminal_reason:"aborted_streaming"`) carrying real `usage`/`modelUsage`/`total_cost_usd`. Fact-finding only; no change to `interrupt`'s mechanism. Full transcripts: `docs/gauntlet/notes/d2-setting-sources-measurement-2026-08-12.md` | 3 bounded `claude-haiku-4-5` turns, killed mid-stream two ways, 2026-08-12 |

**Host convention — the inbox (owner-declared 2026-08-11):** `~/inbox/`
on this host is where the owner drops files for the repo (proposals,
research). A file is deleted from the inbox once accepted/vendored into
the repo — the inbox holds ONLY not-yet-accepted material. Check it on
wake.

Repo invariants (target size, build times, test counts) intentionally NOT
here — they live in CLAUDE.md. Sibling files: `claude-code-cloud.md`,
`github-runner.md`.

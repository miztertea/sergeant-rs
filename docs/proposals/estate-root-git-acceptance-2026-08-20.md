# Estate-root integration — §17 acceptance audit

Date: 2026-08-20. Audited tree: integration/estate-root-git @ 9574286a
(all seven phases merged). Method: five parallel auditors, one per §17
group, each bullet verified against code or a named passing test —
never against commit messages. Result: 27/27 satisfied.

## Estate identity

- **Every estate-scoped command refuses outside an exact valid root before descriptor lookup or spawn.** — satisfied.
  src/cli.rs:559-568 admit_root() is called at dispatch() line 636, before ensure_daemon/spawn_daemon/client_for are ever reached; verified by passing test tests/m8_estate_cli.rs::a_descendant_of_the_estate_root_finds_no_estate_and_spawns_nothing (cargo test --test m8_estate_cli, ok).
- **No code path searches ancestors for an estate in any normal path.** — satisfied.
  src/domain/estate.rs Estate::admit() (line 938) checks only `dir/sergeant.toml`, no parent traversal; repo-wide grep for ancestor-walk patterns (`ancestors()`, `while ... parent()`) returns zero hits outside this admission logic. Pinned by src/domain/estate.rs:2321 test a_plain_git_repository_with_no_manifest_is_no_longer_a_workspace and tests/m8_estate_cli.rs::a_descendant_of_the_estate_root_finds_no_estate_and_spawns_nothing (both pass), which assert a descendant/no-manifest directory finds nothing rather than an ancestor's estate.
- **No code path falls back to a Git repository as a workspace.** — satisfied.
  src/domain/estate.rs:2321 test `a_plain_git_repository_with_no_manifest_is_no_longer_a_workspace` — asserts Estate::admit and Estate::resolve both refuse a plain git repo with no sergeant.toml ("no git fallback survives anywhere"); verified passing via cargo test --lib (ok). No `.git`-as-workspace fallback code found anywhere in src/.
- **A runtime descriptor cannot be used from a different estate root.** — satisfied.
  src/daemon.rs:164 RuntimeDescriptor::check_estate_root, enforced client-side via src/cli.rs check_binding() called before endpoint probe/connect/spawn decision in both ensure_daemon and observe_connect paths; unit test src/daemon.rs:1153 a_descriptor_is_usable_only_from_the_estate_it_is_bound_to verified passing (cargo test --lib, ok) — asserts refusal in both directions (client at a different root, and an unbound daemon serving a scoped client), naming both roots.

## Repository topology

- **Every declared repository resolves only to repos/<name>.** — satisfied.
  src/domain/estate.rs:52 mount_path() derives every repository path as estate_root.join("repos").join(name) with no configurable `path` field (the field is refused by name via EstateError::RepositoryPathDeclared, estate.rs:574-589); enforced by test a_repo_entry_still_declaring_path_is_refused_by_name (estate.rs:1752) and re-enforced at daemon admission via validate_mount (src/runtime/preflight.rs:503).
- **The estate root is never implicitly a Work repository.** — satisfied.
  test domain::estate::tests::a_plain_git_repository_with_no_manifest_is_no_longer_a_workspace (src/domain/estate.rs:2321, verified passing via `cargo test`) pins that a bare git repository root — the old zero-config fallback case — resolves to EstateRootError::NoEstate, not an implicit repository; structurally reinforced because repository paths are only ever mount_path(root, name) = root/repos/<name> (never root itself, since is_plain_name in src/domain/mod.rs:24 rejects '.', '..', and empty names) and EstateError::NoRepositories (estate.rs:1131-1133) refuses a manifest with zero declared [[repo]] entries rather than falling back to the root.
- **A linked worktree, symlinked external checkout, or another estate's clone is refused as a mount.** — satisfied.
  src/domain/estate.rs:833-891 validate_mount() checks (2) canonical git top-level equals the canonical mount — catching symlinks, ../ aliases and another estate's clone (EstateError::RepositoryMountAliased) — and (3) canonical git common-dir lives inside the mount — catching linked worktrees (EstateError::RepositoryMountIsLinkedWorktree); verified passing via `cargo test`: a_symlinked_mount_is_refused_as_an_alias (estate.rs:1669) and a_linked_worktree_is_refused_as_a_repository_source (estate.rs:1708). Same function is reused at daemon admission (src/runtime/preflight.rs:503), so the CLI/manifest-parse path and the daemon path enforce identically rather than duplicating the check.

## Work scope

- **A multi-repo estate cannot submit without explicit scope** — satisfied.
  cargo test resolve_scope_refuses_an_empty_scope_on_a_multi_repository_estate — PASS (src/runtime/engine.rs:4504); enforced by Engine::resolve_scope (src/runtime/engine.rs:1398-1406), which returns EngineError::MissingScope with repo_count/repos/groups when the union of repos+group is empty and estate.repositories.len() != 1.
- **--all is explicit and journaled** — satisfied.
  SubmitContext::all / cli.rs `--all` bool flag (src/cli.rs:118-122, default false, no inference) drives Engine::resolve_scope (src/runtime/engine.rs:1378-1380, wins outright); api.rs:1409 `let scope_request = req.scope.clone();` is stored verbatim on the Work at src/api.rs:1415-1426 into Work::scope_request (ScopeRequest.all, src/domain/work.rs:150-152) and committed with KIND_WORK_SUBMITTED, independent of the resolved repositories list — confirmed by replay test a_current_shape_work_submitted_event_replays_with_no_workspace_label (src/runtime/projection.rs:1506-1543, asserts scope_request.all == false round-trips) and cargo test resolve_scope_all_selects_every_declared_repository_regardless_of_repos_or_group — PASS.
- **Group resolution is identical across CLI, TUI, and direct API clients** — satisfied.
  src/runtime/engine.rs:1374-1412 Engine::resolve_scope is the single daemon-side function; CLI forwards scope.repos/scope.group/scope.all verbatim (src/cli.rs:105-122, comment at src/cli.rs:727-730 naming resolve_scope as the shared reconciliation point); TUI does the same (src/tui/home.rs:235-244, doc comment: 'the form forwards what the operator named and expands nothing ... resolves through the daemon's Engine::resolve_scope'); a direct API client hits the same POST /v1/work path into api.rs:1409/plan() -> Engine::resolve_scope (src/runtime/engine.rs:1237). No client-side group-membership or empty-scope logic exists in cli.rs or tui/home.rs.
- **Existing Work meaning survives later manifest edits** — satisfied.
  Work::repositories is the resolved list pinned once at submit time (src/domain/work.rs:321-327, doc: 'a later manifest edit (a group's membership changes) cannot retroactively change what an already-journaled Work meant'), stored beside the original ScopeRequest (src/domain/work.rs:125-133). resolve_scope is only invoked from Engine::plan at submission (src/runtime/engine.rs:1237); retry (Engine::begin_retry, src/runtime/engine.rs:2168-2186) and rematerialize operate on the already-resolved Work/surface state and never re-call resolve_scope or re-read estate.groups, so a manifest edit after submission cannot alter an in-flight or completed Work's repository set.

## Git base and surface

- **Normal admission requires clean attached HEAD and pins an exact SHA.** — satisfied.
  runtime::preflight::tests::a_clean_attached_mount_is_admitted_with_its_exact_base_pinned (src/runtime/preflight.rs:1304) — ran green (cargo test --lib); asserts base_branch=Some("main"), base_sha == HEAD, is_full_object_id(base_sha), and no waived findings. Enforced by preflight checks 6 (HeadAttached) and 7 (WorktreeClean) in run_checks (src/runtime/preflight.rs ~540-620), which stop admission on the first unwaived finding.
- **Sergeant performs no automatic network or branch-changing git command.** — satisfied.
  tests/e_admission_uses_no_network_git.rs::a_whole_admission_never_runs_a_network_or_branch_changing_git_command — real end-to-end admission through a recording git shim (injectable via runtime::git::GIT_BIN_ENV per C11), asserting none of fetch/pull/push/remote/ls-remote/clone/rebase/merge/cherry-pick/switch/checkout ever run, that the one `reset` materialization performs stays scoped to sergeant's own surface (never the mount), and that no invocation references 'origin' or refs/remotes/*.
- **Dirty/detached override records exactly what was waived and what was excluded.** — satisfied.
  runtime::preflight::tests::an_override_admits_a_dirty_mount_and_records_what_it_excluded (src/runtime/preflight.rs:1334) — pins waived=[WorktreeClean] with full porcelain (' M README.md', '?? untracked.txt') and an explicit uncommitted_excluded message naming the committed SHA the Work is based on; sibling test an_override_admits_a_detached_mount_with_no_named_base_branch pins the detached arm (waived=[HeadAttached], base_branch=None, no exclusion recorded since nothing was dirty). only_dirty_and_detached_are_waivable (line 798) pins the override can waive nothing else.
- **Every selected repository receives exactly one Work branch/worktree.** — satisfied.
  tests/m3_execution.rs::t2_multi_repo_workspace_binds_one_worktree_per_repository — two declared repos produce one surface with two bindings, one worktree each under the surface root, each on its own branch (git rev-parse --abbrev-ref HEAD == sergeant/<work_id>); declaration order preserved. Enforced structurally by materialize_one (src/runtime/surface.rs:773), called once per binding under the interprocess registry lock.
- **Every stage and retry uses the same Work surface/branches.** — satisfied.
  Architectural guarantee: WorkRun.surface is set once at KIND_SURFACE_MATERIALIZED and every stage-entry StartRequest reuses run.surface.clone() for its cwd (src/runtime/engine.rs:3151-3232, `cwd: surface.execution_cwd()`) rather than re-deriving a surface per stage. On failure/retry, rematerialize() (src/runtime/surface.rs:967) re-attaches the SAME retained branch (worktree removed by teardown, branch kept), re-reading only current HEAD — base_branch/base_sha are carried, not re-derived. Confirmed behaviorally by tests/m3_execution.rs::t5_failure_records_the_reason_and_retry_re_enters_the_stage (retry re-enters stage 00-first as attempt 2, same context, work completes). Caveat: no single test directly asserts worktree_path/branch-name equality across two stage attempts within one run (e.g. starts[0].cwd == starts[1].cwd) — the guarantee rests on the single-materialization design plus the retry test rather than a dedicated cross-stage pinning assertion, so this is satisfied by design but only partially pinned by test.
- **Registry operations serialize by common Git directory across processes.** — satisfied.
  tests/c4_repo_lock.rs::a_second_process_holding_the_repository_lock_is_waited_out_not_refused — spawns a real second process holding repolock::acquire via a linked worktree while this process contends via the primary checkout of the same common dir; proves both paths resolve to one lock file (repolock::lock_path) and the contender waits (not refused) until release. Sibling test a_second_process_that_keeps_the_lock_produces_a_bounded_structured_timeout pins the bounded-wait/timeout half. Lock acquisition wraps all registry-mutating surface operations via with_repository (src/runtime/surface.rs:171-176, mutex then repolock::acquire, taken/dropped in the documented eight registry-mutation call sites).

## Integrity and retirement + operations

- **A Worktree ending on another branch cannot be reported clean or represented as though the expected branch contains its output** — satisfied.
  src/runtime/surface.rs teardown_binding (observe_head + AssignedBranchMismatch, ~L1868-1886); test runtime::surface::tests::teardown_reports_a_worktree_that_switched_off_its_work_branch (src/runtime/surface.rs:2949) — verified passing via `cargo test teardown_reports_a_worktree_that_switched_off_its_work_branch`. Asserts observed_head names the actual branch/tip, an assigned_branch_mismatch finding is recorded, and report.integrity() == Dirty even though the binding disposition is Removed.
- **Assigned uncommitted content is preserved under existing fail-closed rules** — satisfied.
  src/runtime/surface.rs retain_dirty/capture_dirty_patch (L1581-1647): patch capture is written durably before any removal, and any capture or removal failure falls back to patch:None with the worktree directory left on disk rather than losing content. Tests: an_uncommitted_worktree_yields_the_finding_beside_the_existing_retention (L3260) and a_dirty_worktree_whose_removal_fails_falls_back_to_the_directory_not_a_leaked_patch (L4448).
- **Detached/unreferenced output is not destroyed** — satisfied.
  src/runtime/surface.rs teardown_binding's AssignedHeadDetachedOrUnreferenced arm (L1895-1918): when reachable_from_a_named_ref is false, teardown returns BindingDisposition::RetainedUnreferenced and never removes the worktree, keeping HEAD as a gc root. Test: runtime::surface::tests::teardown_retains_a_worktree_whose_detached_head_no_named_ref_reaches (L3104) — asserts the worktree still exists and the orphan commit is still in the object store after teardown, and that reap also declines to touch it.
- **Work/Stage state progression is unchanged by implicit integrity checks** — satisfied.
  src/runtime/engine.rs settle_surface's Teardown arm (~L1817-1841): integrity is computed as a sibling key (`report.integrity()`) alongside the KIND_SURFACE_TORN_DOWN payload, not as an input to any WorkState transition; the state machine has no dirty-aware branch. Test: tests/m3_execution.rs a_stranded_completion_is_not_reported_as_plain_completed (L2820) — verified passing; confirms the run genuinely reaches terminal `completed_dirty` (only the reported label changes) and remains truly terminal (retry refuses it, same as plain `completed`).
- **Unattributed mount changes are not falsely charged to a Work** — satisfied.
  src/runtime/integrity.rs DriftAttribution (single-variant Unknown enum, L269-283) and EstateDriftObservation, populated only by observe_estate_drift (src/runtime/surface.rs:1788), which is structurally incapable of returning anything but Unknown. Test: runtime::surface::tests::estate_drift_is_observed_at_teardown_and_never_makes_the_work_dirty (L3514): a mount moved by an outside commit produces a drift observation but report.integrity() stays Clean and findings stay empty.
- **Workflow-required commit behavior remains workflow-owned** — satisfied.
  Amendment-consistent with docs/proposals/estate-root-git.md L35 ("Made visible, not redefined by core"). No non-test code path in src/ invokes `git commit` — every `"commit"` literal outside tests/ is a test-fixture bootstrap (grep across src/*.rs and src/**/*.rs). Directly demonstrated by tests/m3_execution.rs a_stranded_completion_is_not_reported_as_plain_completed: a stage that 'declares a commit, never makes one' strands with the branch never advanced past base_sha — core never commits on the workflow's behalf, it only reports the dirty fact.
- **Work branches remain after every terminal outcome** — satisfied.
  src/runtime/surface.rs teardown_binding/reap never issue `git branch -d/-D` on a Work's own retained branch (the one `branch -D` call at L904 is materialize's own construction-failure rollback before a surface exists, not a terminal-outcome teardown). Verified for all three terminal paths in tests/m3_execution.rs: completed (L1013-1015, 'teardown keeps the branch; it is the durable output'), canceled (L1439, branch_exists asserted true after cancel-during-run teardown), and failed (L3444-3446, 'teardown must retain the branch on failure').
- **sgt doctor reports bounded active/retained/count/size facts without Git ancestry walking or deletion** — satisfied.
  src/cli.rs git_surfaces_check (L2624), doc comment '§12.2: a cheap, bounded Git-surface summary derived from the journal and retained-artifact filesystem metadata only — never a per-branch git ancestry walk'. Implementation only replays the Journal and calls retained_bindings (src/runtime/surface.rs:1285, pure filesystem stat via directory_size/info.path.is_file(), zero git invocations); doctor::run performs no teardown/reap call anywhere. Verified via `cargo test --test m6_surfaces t3_doctor_names_every_fault_and_its_remedy` (passing), which pins the 'git_surfaces' check name/position and its 'no journal yet' ok state.
- **AGENTS.md makes Captain, estate root, mounts, surfaces, and branch ownership unambiguous** — satisfied.
  AGENTS.md L52-58, 'Estate and Git model' table: rows for estate root (Captain), repos/<name> (estate, workers never edit), <surfaces-dir>/<work-id>/<repo> (worker, its actual mutation surface), sergeant/<work-id> (Work, durable output branch retained after every terminal outcome); plus the 'ESTATE — Captain's estate discipline' section (L234-274) spelling out what a worker may/may not do. Cross-checked against real CLI behavior by tests/f_doctrine_skew.rs (all 6 tests pass), including agents_md_session_start_matches_the_real_root_gate and no_embedded_workflow_instructs_a_stage_actor_to_run_estate_scoped_commands_from_its_surface.
- **Embedded distro and binary behavior ship together** — satisfied.
  src/domain/distro.rs L35-38: AGENTS.md/skills/contexts/workflows are pulled in at compile time via `include_str!`/`include_dir!` (not read from disk at runtime, not cloned) and written into a fresh estate by `sgt init` (write_distro, L67). Doc comment cites ADR 0014 decision 1 verbatim. Consistency with the shipped binary's own root-gate/help text is enforced by tests/f_doctrine_skew.rs (all passing, e.g. embedded_skills_carry_the_real_root_and_preflight_remedies).
